import React from 'react';
import {useDispatch} from "react-redux";


const Pagination = ({  }) => {
    const dispatch = useDispatch()

    return (
        <div>

        </div>
    );
};

export {Pagination};